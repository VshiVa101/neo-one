# Piano: Bilanciamento audio definitivo (Hero/RUMORE vs Sito)

## Obiettivo
- **Hero + RUMORE**: SOLO static CRT sintetizzato + `fruscio-satanico.mp3`
- **Sito (home/calendar)**: `banana.mp3` come musica di background (sostituisce l'attuale Cloudinary URL)

## File da modificare

### 1. `src/utilities/crtNoiseManager.ts`
- Rimuovere `BANANA_URL`, `BANANA_VOLUME`
- Rimuovere `bananaAudio` (variabile)
- Rimuovere creazione e play di `bananaAudio` da `startCrtNoise()`
- Rimuovere stop e cleanup di `bananaAudio` da `stopCrtNoise()`
- Restano: static CRT + fruscio-satanico

### 2. `src/contexts/AudioContext.tsx`
- Cambiare `BG_MUSIC_URL` da `https://res.cloudinary.com/.../background-music.mp3` a `'/media/banana.mp3'`

### 3. `src/app/(frontend)/artwork/[...nid]/ArtworkDetailClient.tsx`
- Nessuna modifica necessaria — già chiama `fadeOutAndPause()` + `startCrtNoise()` / `stopCrtNoise()` + `restartFromStart()`

### 4. `src/app/(frontend)/HeroClient.tsx`
- Nessuna modifica necessaria — già chiama `stopMusicImmediately()` + `startCrtNoise()` al mount

## Riepilogo contesti audio

| Contesto | BG Music (banana.mp3) | Static CRT | Fruscio (0.5) |
|----------|----------------------|------------|---------------|
| Hero     | SPENTA               | ON         | ON            |
| RUMORE   | dissolve → SPENTA    | ON         | ON            |
| Home/Calendar | ON (0.5)       | OFF        | OFF           |

## Note
- `banana.mp3` è già presente in `public/media/banana.mp3` (copiato da `No.-Meat-Banana-2-.mp3`)
- `fruscio-satanico.mp3` è in `public/media/fruscio-satanico.mp3` (volume 0.5)
- Lo static CRT è rumore bianco + hum 50Hz + buzz 100Hz sintetizzato via Web Audio API

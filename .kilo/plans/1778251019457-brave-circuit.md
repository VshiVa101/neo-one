# Piano d'Azione — Front-End Neo-One Art Hub

## Goal
Rimuovere il debito tecnico critico dal front-end di Neo-One Art Hub (Next.js 15, Tailwind CSS v4, Framer Motion, Three.js/R3F) secondo le raccomandazioni dell'audit senior.

## Fasi di Implementazione (3 Batch)

---

## Fase 1 — Rimuovere il collo di bottiglia principale (Potrebbe richiedere più sessioni)

### 1.1 Rimuovere `NeoBrandingObserver.tsx` (il MutationObserver killer)
*   **File da ELIMINARE**:
    *   `src/components/NeoBrandingObserver.tsx` → **DELETE**. È il MutationObserver che scansiona tutto il DOM. Colpevole del 90% dei problemi di performance.
*   **File da MODIFICARE**:
    *   `src/app/(frontend)/layout.tsx` → rimuovere `import { NeoBrandingObserver }` e il `<NeoBrandingObserver />` nel JSX.
*   **File da NON toccare** (sono legittimi):
    *   `src/components/BrandedTitle.tsx` → **TENERE**. È un componente React che wrappa O,N,E in `<span>` colorati. Nessun impatto performance, è puramente dichiarativo. Usato da ClusterLayout, ArtworkDetailClient, CartContext.
    *   `src/utilities/normalizeNeoText.ts` → **TENERE**. Serve per normalizzare caratteri accentati non supportati dal font Neo. Usato da BrandedTitle e CartContext.
*   **Come Fixare il branding**:
    *   Il CSS globale con `!important` e regole come `*:not(.neo-skip-branding)` esisteva SOLO per supportare il MutationObserver. Dopo l'eliminazione di NeoBrandingObserver, rimuovere:
        *   I selettori universali con `text-transform: lowercase !important` e `color: white !important` da `globals.css`.
        *   I selettori `[data-neo-skip]` e `neo-skip-branding` non servono più.
    *   Mantenere le classi `.neo-O`, `.neo-N`, `.neo-E` in `globals.css` (sono usate da `BrandedTitle.tsx`).
    *   Rimuovere gli attributi `data-neo-skip="true"`, `className="neo-skip-branding"` da TUTTO il JSX (ClusterLayout, CartContext, ArtworkDetailClient, EyeScene.tsx, ecc.).
    *   Per il testo che deve restare in minuscolo, aggiungere esplicitamente `lowercase` di Tailwind dove serve.

### 1.2 Sostituire `<img>` con `<Image>` Next.js
*   **File Aggressore**:
    *   `src/components/home/ClusterLayout.tsx` → sostituire `<img>` con `<Image>` per le immagini dei cluster.
    *   `src/components/home/ClusterDeck.tsx` → sostituire `<img>` nella card artwork con `<Image>`.
    *   `src/components/home/ExpandedGalleryOverlay.tsx` → sostituire `<img>` nella griglia con `<Image>`.
    *   `src/components/TransitionOverlay.tsx` → sostituire `<img>` con `<Image>` per le GIF esplosione.
    *   `src/app/(frontend)/artwork/[...nid]/page.tsx` → sostituire `<img>` background.
    *   `src/app/(frontend)/artwork/[...nid]/ArtworkDetailClient.tsx` → sostituire `<img>` central artwork.
    *   `src/app/(frontend)/cluster/[slug]/page.tsx` → sostituire `<img>` grid artwork.
    *   `src/app/(frontend)/home/page.tsx` → sostituire `<img>` background GIF con `<Image>`.
*   **Configurazione necessaria**:
    *   Verificare che `next.config.ts` abbia `images.remotePatterns` configurato per Cloudinary e il server locale.
    *   Se le immagini sono decorative e non devono essere ottimizzate, usare `unoptimized` o `priority` dove opportuno.

### 1.3 Pulire `globals.css` (rimuovere regole globali aggressive)
*   **Rimuovere da `globals.css` (`src/app/(frontend)/globals.css`)**:
    *   Togliere TUTTO il blocco (linee ~252-266):
        ```css
        /* Absolute Branded Typography Enforcement */
        *:not(.neo-skip-branding):not(.neo-skip-branding *):not([class^="neo-"]) {
          text-transform: lowercase !important;
          color: white !important;
        }
        [data-neo-skip] text,
        [data-neo-skip] span,
        .neo-skip-branding text,
        .neo-skip-branding span {
          text-transform: uppercase !important;
          color: #809829 !important;
          fill: #809829 !important;
        }
        ```
    *   Perché: queste regole esistevano per supportare il MutationObserver. Con NeoBrandingObserver rimosso, diventano solo un ostacolo.
*   **Tenere in `globals.css`**:
    *   Le classi `.neo-O`, `.neo-N`, `.neo-E` (usate da `BrandedTitle.tsx`).
    *   Le classi `.crt-screen`, `.custom-scrollbar`, `.neo-interface-btn`, keyframes.
*   **Pulire da TUTTO il JSX** (rimuovere attributi inutili):
    *   `ClusterLayout.tsx` (linee 326, 367): rimuovere `neo-skip-branding` dalla className.
    *   `ArtworkDetailClient.tsx` (linee 288, 291, 294, 301, 304): rimuovere `neo-skip-branding`.
    *   `CartContext.tsx` (linea 167): rimuovere `data-neo-skip="true"`.
    *   `CartContext.tsx` (linea 169): rimuovere `neo-skip-branding`.
    *   `EyeScene.tsx` (linee 201-202, 205-206): rimuovere `neo-skip-branding` e `data-neo-skip="true"` dal CircularText HTML.
*   **Verifica**: Dopo la pulizia, il testo si comporterà normalmente — ogni componente decide il proprio `text-transform` e `color` via Tailwind utility.

---

## Fase 2 — Riorganizzare il Codice (Più facile, potrebbe richiedere una seconda sessione)

### 2.1 Spezzare componenti monolitici
*   **Target**: `src/components/home/ClusterLayout.tsx` (612 righe)
*   **Cosa Fare**:
    1. Creare `ClusterNavFooter.tsx` per il footer scrollabile delle thumbnail.
    2. Creare `ClusterMainStage.tsx` per la sezione centrale con i due cluster e le descrizioni.
    3. Creare `ExpandedClusterModal.tsx` per l'overlay espanso di un cluster (anche se attualmente è inline in ClusterLayout).
    4. Estrarre il gestore wheel nella galleria in un hook custom `useGalleryWheel`.
*   **Target**: `src/components/home/ClusterDeck.tsx` (202 righe)
*   **Cosa Fare**: Spezzare in `DeckCard.tsx`, `DeckHeader.tsx`. Separare la logica di stile nella funzione `getCardStyle` in un helper dedicato.

### 2.2 Consolidare logica condivisa
*   Estrarre `ScrambleChar` e `MATRIX_TEXT` da `MatrixGateway.tsx` e `MiniMatrixLoader.tsx` in un singolo componente/hook in `src/components/home/ScrambleChar.tsx` o `src/hooks/useMatrixText.ts`.
*   Creare un util `timers.ts` per centralizzare costanti di timeout/debounce.

### 2.3 Refattorizzare CSS
*   Spostare stili inline da `style={{ ... }}` in classi Tailwind.
*   Rimuovere le proprietà `-webkit-mask-*` inline e spostarle in una classe CSS `@layer utilities` o in un CSS module.
*   Standardizzare `z-index` scale: creare costanti o `@layer` per overlay/modal/eye.

---

## Fase 3 — Best Practice JS & a11y (Facile, può essere fatto nella stessa sessione di Fase 2)

### 3.1 Fix JS
*   In `EyeScene.tsx`, sostituire `setTimeout(() => onReady(), 100)` con `requestAnimationFrame`.
*   Rimuovere i `console.log`/`console.error` da `actions.ts` o wrapparli in un logger condizionale.
*   Aggiungere `useRef` per tutti i timer in `MatrixGateway.tsx` per evitare memory leaks su unmount.
*   Rivedere i `useEffect` con cleanup di event listeners: alcuni hanno strutture non perfette (es. cleanup di EyeScene potrebbe non rilasciare WebGL context).

### 3.2 Fix a11y
*   **Keyboarding**: Aggiungere `role="button"`, `tabIndex={0}`, `onKeyDown={(e) => e.key === 'Enter' && onClick()}` a `<MatrixGateway>` e simili elementi cliccabili.
*   **Immagini decorative**: Aggiungere `alt=""` (vuoto) e `role="presentation"` alle immagini decorative come sfondi.
*   **Caroselli**: Aggiungere `aria-roledescription="carousel"`, `aria-label` e gestire il focus loop per i deck di opere.
*   **Lingua**: Cambiare `<html lang="en">` in `<html lang="it">`.

---

## Verifica Post-Implementazione

### Dopo Fase 1
- [ ] `npm run lint` — nessun errore
- [ ] `npm run dev` — applicazione si avvia senza crash
- [ ] Home page: il branding O-N-E funziona via `BrandedTitle` (non più via DOM mutation)
- [ ] Home page: immagini cluster caricate con lazy loading (inspect Network tab)
- [ ] Nessun regola `!important` superstite in `globals.css`
- [ ] Nessun attributo `neo-skip-branding` o `data-neo-skip` nel JSX

### Dopo Fase 2
- [ ] `ClusterLayout.tsx` ridotto sotto le 300 righe
- [ ] `ClusterDeck.tsx` ridotto sotto le 120 righe
- [ ] `ScrambleChar` non è più duplicato in MatrixGateway e MiniMatrixLoader
- [ ] Stili `WebkitMaskImage` spostati in CSS module o `@layer utilities`

### Dopo Fase 3
- [ ] MatrixGateway navigabile con Tab e attivabile con Enter
- [ ] Tutte le immagini decorative hanno `alt=""` o `role="presentation"`
- [ ] HTML ha `lang="it"`
- [ ] Lighthouse score: Performance > 70, Accessibility > 85
- [ ] Test su mobile: swipe caroselli e touch ancora funzionanti
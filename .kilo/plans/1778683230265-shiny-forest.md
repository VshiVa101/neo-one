# Plan: Pulizia Placeholder Calendar/Event Detail

## Obiettivo
Rimuovere da `EventDetail.tsx` tutti i testi/decorazioni hardcoded che non provengono da dati Payload, e pulire il mapping dead `stickers` in `page.tsx`.

## Analisi
I dati reali Payload (`signals` collection) forniscono: `title`, `description`, `eventCTA`, `previewImage`, `detailImages`.
Il `NeoEvent` interface ha un campo `stickers: string[]` senza corrispondenza in Payload, mappato a `[]` fisso in `page.tsx`.

## File da modificare

### 1. `src/components/calendar/EventDetail.tsx` — 3 placeholder da rimuovere

| Riga | Contenuto hardcoded | Azione |
|------|---------------------|--------|
| 102-103 | `<BrandedTitle text="stampa a mano!" />` (label sotto immagine primaria) | Rimuovere l'intero `<div>` contenitore (righe 102-104) |
| 132-143 | `<BrandedTitle text="e ricamata coi piedi da una suora!" />` (bolla burst rosa) | Rimuovere l'intero blocco `motion.div` della bolla rosa |
| 157-168 | `<BrandedTitle text="Stupidi gadget in omaggio!" />` (sticker nero strappato) | Rimuovere l'intero blocco `motion.div` dello sticker nero |

Nota: il `comicBubble` vero (righe 170-181) è già data-driven (`event.details.comicBubble`) e **NON** va toccato.

### 2. `src/app/(frontend)/calendar/page.tsx` — 1 cleanup

| Riga | Contenuto | Azione |
|------|-----------|--------|
| 49 | `stickers: [],` | Rimuovere questa riga dal mapping Payload → NeoEvent |

### 3. `src/data/calendar-mock.ts` — cleanup interfaccia

| Riga | Contenuto | Azione |
|------|-----------|--------|
| 14 | `stickers: string[]` nel `NeoEvent.details` | Rimuovere il campo dall'interfaccia (non ha corrispondenza Payload) |

## Non toccare
- `comicBubble` rendering (righe 170-181 EventDetail) — è già data-driven
- `ContactForm.tsx` — è un form funzionale, nessun placeholder
- `TornPaper.tsx` — componente decorativo puro, nessun testo hardcoded
- `CalendarClient.tsx` — nessun placeholder, tutto data-driven
- File mock come reference — restano invariati

## Ordine esecuzione
1. `EventDetail.tsx` — rimozione 3 blocchi placeholder
2. `page.tsx` — rimozione `stickers: []`
3. `calendar-mock.ts` — rimozione campo `stickers` dall'interfaccia
